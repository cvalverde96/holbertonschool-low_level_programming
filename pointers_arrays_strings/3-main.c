#include "main.h"

/**
 * main - imprime un string
 * Return: 0
 */

int main(void)
{
	char *str;

	str = "I do no fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
