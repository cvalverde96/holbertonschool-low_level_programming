#include "main.h"

/**
 * main - reverse string
 * Return: 0 es exito
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
